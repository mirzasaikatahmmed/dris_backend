model LightspeedToken {
  id           Int      @id @default(1)
  singleton    Boolean  @unique @default(true)
  accessToken  String
  refreshToken String
  tokenType    String
  expiresIn    DateTime
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@map("lightspeed_token")
}
